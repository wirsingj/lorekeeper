# Prompt Builder

The prompt builder turns focused campaign context packs into provider-ready prompts.

- `templates.js` contains the table DM turn template.
- `build-prompt.js` renders a full prompt with a structured LoreKeeper update contract.

Provider responses should include proposed updates, not direct canon mutations.
